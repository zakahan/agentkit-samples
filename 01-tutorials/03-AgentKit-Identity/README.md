<!-- markdownlint-disable -->
# Agent Identity Notebook 导览文档

欢迎使用 Agent Identity Notebook 项目！本项目包含了一系列 Jupyter Notebook，用于演示和学习 Agent Identity 身份认证服务在不同场景下的应用。

## 📚 项目概述

本项目提供了完整的 Agent Identity 身份认证解决方案示例，涵盖入站认证（Inbound Auth）和出站认证（Outbound Auth）的各种场景。每个 notebook 都是一个独立的教程，包含详细的代码示例和说明文档。

## 🗂️ 目录结构

```
03-AgentKit-Identity/
├── 03-Inbound_Auth/                   # 入站认证 - 用户认证
├── 04-Outbound_Auth/                  # 出站认证 - API密钥方式
├── 05-Outbound_Auth_3lo_lark/         # 出站认证 - OAuth 3LO (Lark)
└── utils.py                           # 通用工具函数
```

## 📖 Notebook 场景说明

### 🔐 入站认证 (Inbound Authentication)

#### 1. Inbound_Auth - 用户身份认证
- **文件**: `03-Inbound_Auth/inbound_auth_runtime_with_veidentity_and_veadk.ipynb`
- **场景**: 验证用户身份，允许用户访问 AgentKit Runtime 中的 agents/tools
- **认证方式**: OAuth2 用户认证
- **适用场景**: 
  - 用户通过 Web 应用访问 AI Agent
  - 需要验证最终用户身份的场景
  - B2C 应用场景
- **关键特性**:
  - 使用 Agent Identity 用户池进行用户管理
  - OAuth2 授权码流程
  - 支持用户权限控制

### 🔐 出站认证 (Outbound Authentication)

#### 2. Outbound_Auth - API密钥认证
- **文件**: `04-Outbound_Auth/outbound_auth_runtime_with_veidentity_and_veadk.ipynb`
- **场景**: Agent 使用 API 密钥访问外部服务
- **认证方式**: API Key 凭据提供商
- **适用场景**:
  - 访问支持 API 密钥的第三方服务
  - 简单的服务集成
  - 不需要用户授权的场景
- **关键特性**:
  - 集中管理 API 密钥
  - 安全的密钥存储和检索
  - 支持多种第三方服务

#### 3. Outbound_Auth_3lo_lark - OAuth 3LO (Lark)
- **文件**: `05-Outbound_Auth_3lo_lark/outbound_auth_runtime_3lo_with_veidentity_and_veadk.ipynb`
- **场景**: Agent 代表用户访问飞书 (Lark) 服务
- **认证方式**: OAuth 2.0 三方授权 (3-Legged OAuth)
- **适用场景**:
  - 飞书文档读取和操作
  - 企业内部应用集成
  - 办公自动化场景
- **关键特性**:
  - 飞书 API 集成
  - 文档权限管理
  - 自定义 OAuth 提供商配置

## 🚀 快速开始

### 环境准备
1. 确保已安装 Python 3.10+ 以及 [uv](https://docs.astral.sh/uv/#scripts)
2. 安装 Jupyter Notebook 或 JupyterLab
3. 安装项目依赖（每个目录下都有 `requirements.txt`）

### 选择合适的场景
根据您的需求选择对应的 notebook：

- **用户认证场景** → `Inbound_Auth`
- **简单第三方服务集成** → `Outbound_Auth`
- **飞书集成** → `Outbound_Auth_lark`

### 运行步骤
1. 进入对应的目录
2. 复制环境变量配置文件：`cp .env.example .env`
3. 编辑 `.env` 文件，填入您的实际配置信息：
   - `VOLCENGINE_ACCESS_KEY`: 火山引擎访问密钥
   - `VOLCENGINE_SECRET_KEY`: 火山引擎密钥
   - `MODEL_API_KEY`: 火山方舟 API 密钥
   - 其他特定服务的 API 密钥（如需要）
4. 启动 Jupyter：`jupyter notebook`
5. 打开对应的 `.ipynb` 文件
6. 按照 notebook 中的说明逐步执行

## 🔧 配置说明

每个场景都需要相应的配置：

- **Agent Identity 配置**: 用户池、工作负载池、凭据提供商
- **第三方服务配置**: Client ID、Client Secret、API Key
- **火山引擎配置**: 区域、服务端点等

详细配置步骤请参考各个 notebook 中的说明。

## 📋 技术栈

- **身份认证**: Agent Identity
- **Agent 框架**: VeADK (Volcano Engine Agent Development Kit)
- **开发语言**: Python
- **SDK**: AgentKit Python SDK, VeADK, volcengine-python-sdk

---

**注意**: 在运行任何 notebook 之前，请确保您已经正确配置了 Agent Identity 服务和相关的第三方服务凭据。